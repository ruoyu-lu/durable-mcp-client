# Project review — 2026-09-21

Baseline: main at 4e8c7b7, clean and matching origin. No open PRs/issues before this review; no GitHub release. PRs #5–#14 contain real runtime/example changes. The core is 489 TypeScript lines across seven files. This is a working development CLI, not a packaged release or host integration.

## Verification

Reran TypeScript check/build, all 39 runtime/compatibility tests and the real FastMCP integration successfully. `npm pack --dry-run --json` listed 31 files and no dist. No code behavior is changed by this review; it is an explicitly requested planning/documentation deliverable.

## Findings

| Priority | Finding and evidence | Action |
| --- | --- | --- |
| R1 first | TaskStore.observe accepts an arbitrary status when it is valid JSON; recordError can add a late network failure after completed. Both reproduced with in-memory records. | #15: state validation, transactional terminal-error guard and race tests |
| R1 | Every respond exception becomes unknown; a second, corrected answer is blocked forever. Reproduced with a deterministic rejecting adapter. This is not a claim that a live FastMCP rejection was tested. | #16: classified errors and narrowly safe explicit correction |
| R1 | Live example hard-codes memory:// and keeps the server alive throughout tests. | #17: Redis server-restart reattachment without local result cache |
| R2 | package.json is private at 0.0.0, lacks bin/exports/files/build-on-pack; artifact omits dist. SDK dependency is used only by probes. | #18: deliberate CLI artifact and clean installation test |
| Documentation | README described caller/session identity and outbox as present; roadmap still put delivered work behind M0. | Corrected implemented scope, architecture and release gates |
| Planning | Recent runs followed a single handoff action, often splitting adjacent small features across days, without a release gate or weekly reprioritization. | Milestone-based selection; finish runnable slices without a one-PR-per-run target |
| Review | Recent PRs have no formal approval; private notes repeatedly reuse the earlier Copilot quota failure without a new availability check. | Request review when available, date failures, recheck after a bounded cooldown, record actual fallback review |

## Resulting direction

R1 reliable recovery -> R2 installable CLI alpha -> R3 demand-driven interoperability -> optional R4 host integration. Current detailed work is tracked in issues #15–#18 and GitHub milestones R1/R2. New priority is #15, before the previously queued Redis example.

Scheduled execution stays daily at the existing time in the existing thread. The instruction source is the live scheduled prompt, which now reads roadmap/backlog and recent evidence rather than repeating completed initialization tasks. Private continuity must record the current gate, exact checks, review attempt date/status and one next action; it must not override the backlog. Weekly reassessment updates stale claims and priorities, without requiring daily documentation churn.

Remaining limits include no auth/SSE, remote expiry classification, active-job restart checkpoints, response retry reconciliation, host delivery or published artifact. Fixed-interval polling enhancements are useful but no longer the top release blocker.
